name: CI Frontend

on:
  push:
    branches:
      - main
      - audit-fixes
  pull_request:
    branches:
      - main
      - audit-fixes

jobs:
  no-doc-files:
    name: Block markdown and txt files
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Fail on mojibake patterns
        run: |
          set -euo pipefail
          export MOJI_PATTERN='(\x{00C3}|\x{00C4}|\x{00C5}|\x{00C2}|\x{00E2}|\x{FFFD})'

          should_skip_path() {
            case "$1" in
              vendor/*|node_modules/*|dist/*|.venv/*|storage/*|bootstrap/cache/*) return 0 ;;
              *) return 1 ;;
            esac
          }

          collect_targets() {
            if [ "${GITHUB_EVENT_NAME}" = "pull_request" ]; then
              git fetch --no-tags --depth=1 origin "${GITHUB_BASE_REF}"
              git diff --name-only --diff-filter=ACMR "origin/${GITHUB_BASE_REF}...HEAD"
            else
              BEFORE_SHA="${{ github.event.before }}"
              if [ -n "${BEFORE_SHA}" ] && [ "${BEFORE_SHA}" != "0000000000000000000000000000000000000000" ]; then
                git diff --name-only --diff-filter=ACMR "${BEFORE_SHA}" "${GITHUB_SHA}"
              else
                git diff-tree --no-commit-id --name-only -r --diff-filter=ACMR "${GITHUB_SHA}"
              fi
            fi
          }

          mapfile -t CANDIDATES < <(collect_targets)
          TARGETS=()
          for path in "${CANDIDATES[@]}"; do
            [ -f "$path" ] || continue
            should_skip_path "$path" && continue
            case "$path" in
              *.php|*.py|*.vue|*.ts|*.js) TARGETS+=("$path") ;;
            esac
          done

          if [ "${#TARGETS[@]}" -eq 0 ]; then
            echo "No candidate files for mojibake scan."
            exit 0
          fi

          if command -v rg >/dev/null 2>&1; then
            if rg -n --pcre2 "${MOJI_PATTERN}" -- "${TARGETS[@]}"; then
              echo "Mojibake patterns detected. Please re-save affected files as UTF-8."
              exit 1
            fi
            exit 0
          fi

          python - "${TARGETS[@]}" <<'PY'
          import pathlib
          import re
          import sys

          pattern = re.compile(r"(\u00C3|\u00C4|\u00C5|\u00C2|\u00E2|\uFFFD)")
          hits = []

          for f in sys.argv[1:]:
              p = pathlib.Path(f)
              try:
                  text = p.read_text(encoding="utf-8", errors="replace")
              except Exception:
                  continue
              for i, line in enumerate(text.splitlines(), 1):
                  if pattern.search(line):
                      hits.append((f, i, line.strip()))

          if hits:
              for f, i, line in hits:
                  print(f"{f}:{i}: {line}")
              print("Mojibake patterns detected. Please re-save affected files as UTF-8.")
              raise SystemExit(1)
          PY


      - name: Fail when markdown or txt files exist
        run: |
          set -euo pipefail

          should_skip_path() {
            case "$1" in
              vendor/*|node_modules/*|storage/*|docs/*|.pytest_cache/*) return 0 ;;
              README.md|SECURITY.md|security.md|requirements.txt) return 0 ;;
              *) return 1 ;;
            esac
          }

          collect_targets() {
            if [ "${GITHUB_EVENT_NAME}" = "pull_request" ]; then
              git fetch --no-tags --depth=1 origin "${GITHUB_BASE_REF}"
              git diff --name-only --diff-filter=ACMR "origin/${GITHUB_BASE_REF}...HEAD"
            else
              BEFORE_SHA="${{ github.event.before }}"
              if [ -n "${BEFORE_SHA}" ] && [ "${BEFORE_SHA}" != "0000000000000000000000000000000000000000" ]; then
                git diff --name-only --diff-filter=ACMR "${BEFORE_SHA}" "${GITHUB_SHA}"
              else
                git diff-tree --no-commit-id --name-only -r --diff-filter=ACMR "${GITHUB_SHA}"
              fi
            fi
          }

          mapfile -t CANDIDATES < <(collect_targets)
          FORBIDDEN_FILES=()
          for path in "${CANDIDATES[@]}"; do
            [ -n "$path" ] || continue
            should_skip_path "$path" && continue
            case "$path" in
              *.md|*.MD|*.txt|*.TXT) FORBIDDEN_FILES+=("$path") ;;
            esac
          done

          if [ "${#FORBIDDEN_FILES[@]}" -gt 0 ]; then
            echo "Found forbidden documentation files:"
            printf '%s\n' "${FORBIDDEN_FILES[@]}"
            exit 1
          fi

  frontend:
    name: Vue lint, test, build
    runs-on: ubuntu-latest
    needs: no-doc-files
    defaults:
      run:
        working-directory: frontend

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Unit tests
        run: npm run test

      - name: Build
        run: npm run build

  frontend-e2e-smoke:
    name: Frontend E2E smoke (Playwright)
    runs-on: ubuntu-latest
    needs: no-doc-files
    defaults:
      run:
        working-directory: frontend

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium

      - name: Run smoke E2E
        run: npm run test:e2e:smoke
